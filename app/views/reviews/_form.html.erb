<%= form_for([@item,@item.reviews.build]) do |form|  %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :review_content %>
  </div>

  <div class="field">

    <%= form.hidden_field :user_id, id: :review_user_id %>
  </div>

  <div class="field">
  
    <%= form.hidden_field :item_id, id: :review_item_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
