
<div class="container">
  <p id="notice"><%= notice %></p>
  <div class="row justify-content-around mt-5">
    <div class="col-lg-3 col-md-4 col-sm-12 col-xs-12">
      <img src="<%= @profile.image_url %>" height="200" width="200">



    </div>
    <div class="col-lg-9 col-md-4 col-sm-12 col-xs-12">
      <div class="greeting">
        Hey, I’m <%= @profile.first_name %>
      </div>
      <i class="fa fa-map-marker" aria-hidden="true"></i>
      <%= @profile.city %>,
      <%= @profile.state %>
      <p class="mt-3">Dès l’âge de 7 ans, mes parents m’ont certainement offert le plus beau cadeau …Une machine à coudre (pour enfant) ! Le rêve : partager la même passion que ma maman …Mon mentor.</p>
    </div>
  </div>

  <div class="row profile-heading mt-5">
    <h4>My posts<h4><i class="ml-2 fa fa-tags" aria-hidden="true"></i>
  </div>
  <div class="row mt-5">

    <% @my_items.each do |item| %>
    <div class="col-lg 3 col-md-3 col-sm-12 col-xs-12">
      <div class="card mb-5">
        <%= link_to item_path(item) do %>
        <%= image_tag item.image_url , class: "card-img-top img-fluid"%>
        <% end %>
        <div class="card-block">
          <p class="text-h4"><%= item.name %></p>
          <p class="text-h4 text-primary"> $ <%= item.price %></p>
        </div>
      </div>
    </div>
    <% end %>
  </div>


  <div class="row profile-heading mt-5">
    <h4>My Likes<h4><i class="ml-2 fa fa-heart" aria-hidden="true"></i>
  </div>
  <div class="row mt-5">

    <% @my_liked_items.each do |item| %>
    <div class="col-lg 3 col-md-3 col-sm-12 col-xs-12">
      <div class="card mb-5">
        <%= link_to item_path(item) do %>
        <%= image_tag item.image_url , class: "card-img-top img-fluid"%>
        <% end %>
        <div class="card-block">
          <p class="text-h4"><%= item.name %></p>
          <p class="text-h4 text-primary"> $ <%= item.price %></p>
        </div>
      </div>
    </div>
    <% end %>
  </div>

  <div class="row profile-heading mt-5 mb-2">
    <i class="fa fa-map-marker fa-2x mr-2" aria-hidden="true"></i><h4>Location<h4>
  </div>

  <iframe width="800"
    height="450" src = "https://maps.google.com/maps?q=<%= @profile.latitude %>,<%= @profile.longitude %>&hl=es;z=14&amp;output=embed"></iframe>


</div>

























<!-- <iframe width="800"
  height="450" src = "https://maps.google.com/maps?q=<%= @profile.latitude %>,<%= @profile.longitude %>&hl=es;z=14&amp;output=embed"></iframe> -->

<!-- Google Map -->

		<!-- <div class="row">
			<div class="col-md-12">
				<div id="map">

				</div>
				<style>
					#map{
						width: 100%;
						height: 400px;
					}
				</style>
				<script src="https://maps.googleapis.com/maps/api/js"></script>
			    <script> -->
		<!-- // 	      function initialize() {
		// 	        var mapCanvas = document.getElementById('map');
		// 	        var mapOptions = {
		// 	          center: new google.maps.LatLng(<%= @profile.latitude %>, <%= @profile.longitude %>),
		// 	          zoom: 14,
		// 	          mapTypeId: google.maps.MapTypeId.ROADMAP
		// 	        }
		// 	        var map = new google.maps.Map(mapCanvas, mapOptions);
		// 	        var marker = new google.maps.Marker({
		// 	        	position: new google.maps.LatLng(<%= @profile.latitude %>, <%= @profile.longitude %>),
		// 	        	title: "Airbnb Clone"
		// 	        });
		// 	        marker.setMap(map);
		// 	      }
		// 	      google.maps.event.addDomListener(window, 'load', initialize);
		// 	    </script>
    //  -->
		<!-- // 	</div>
		// </div> -->






<%= link_to 'Edit', edit_profile_path(@profile) %> |
<%= link_to 'Back', profiles_path %>
